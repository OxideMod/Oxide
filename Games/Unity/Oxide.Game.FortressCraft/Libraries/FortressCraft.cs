using Oxide.Core.Libraries;

namespace Oxide.Game.FortressCraft.Libraries
{
    /// <summary>
    /// A library containing utility shortcut functions for FortressCraft
    /// </summary>
    public class FortressCraft : Library
    {
        /// <summary>
        /// Returns if this library should be loaded into the global namespace
        /// </summary>
        public override bool IsGlobal => false;
    }
}
